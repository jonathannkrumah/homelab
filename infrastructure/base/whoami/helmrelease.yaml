apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: whoami
  namespace: whoami
spec:
  chart:
    spec:
      chart: whoami
      version: 1.6.0
      sourceRef: 
        kind: HelmRepository
        name: whoami
        namespace: flux-system
  values:
    service:
      type: loadbalancer 